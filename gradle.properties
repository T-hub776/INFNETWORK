# Enable AndroidX support so the compiler can read modern layout libraries
android.useAndroidX=true

# Allocate memory limits to keep the automated GitHub Actions server running fast
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
